const size = {
    sms: "320px",
    sm: "480px",
    mds: '481px',
    md: '690px',
    lgs: '768px',
    lg: '1100px',
}

export const breakpoints = {
    sms: `(max-width: ${size.sms})`,
    sm: `(max-width: ${size.sm})`,
    mds: `(max-width: ${size.mds})`,
    md: `(max-width: ${size.md})`,
    lgs: `(max-width: ${size.lgs})`,
    lg: `(max-width: ${size.lg})`,
}